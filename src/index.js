const restapi = require('./controllers/RestApi');

// const controllerDatabase = require('./data/DbContext')();

// async function Teste() {
//     var logs = await controllerDatabase.then(async controller => await controller.select("select codigo, nome from cliente"));
//     console.log(logs);
// }
// Teste()

restapi();